"""PySide6-бэкенд UI (в разработке, этапы 3–8 soft-sniffing-allen.md).

Активируется флагом ``python main.py --ui pyside6``. До завершения Этапа 8
живёт параллельно с ``ui.tkinter_ui`` — переключение по CLI-флагу.
"""
