"""System-level helpers: SessionManager (M2), screenshots (M3), volume (M5).

Empty in M1 — the package exists so commands can import lazily without
hitting ``ModuleNotFoundError`` once the real implementations land.
"""
