[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: долгие интеграционные тесты (загрузка моделей, реальный аудио-турн)
    gpu: требуют CUDA (Whisper/XTTS реально на GPU)
filterwarnings =
    ignore::DeprecationWarning
